apiVersion: v1
kind: Service
metadata:
  name: {{ include "microplatform.fullname" . }}
  labels:
    {{- include "microplatform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microplatform.selectorLabels" . | nindent 4 }}
