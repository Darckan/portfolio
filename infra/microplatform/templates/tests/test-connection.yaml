apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microplatform.fullname" . }}-test-connection"
  labels:
    {{- include "microplatform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microplatform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
